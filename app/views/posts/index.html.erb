<% if @posts.any? %>
  <% @posts.each do |post| %>
    <%= link_to "#{post.name}", post_path(post) %>
      <h2> <%= post.name %> </h2>
  <% end %>
<% else %>
  No posts yet!
<% end %>

<a href='/posts/new'>Add a post!</a>